{
  "name": "candlcrew-training-game",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step needed'",
    "deploy": "netlify deploy --prod",
    "test": "curl http://localhost:8888",
    "git:commit": "git add . && git commit -m",
    "git:status": "git status",
    "git:log": "git log --oneline"
  },
  "dependencies": {
    "hono": "^4.9.8"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0",
    "typescript": "^5.0.0"
  }
}